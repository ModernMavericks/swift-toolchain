ModernMavericks swift-toolchain 6.3.3-mavericks.2.

- Repackage of the pinned Swift 6.3.3 build environment. No change to `pins.env`:
  same `SWIFT_VERSION`, same `SWIFT_SHA`, same `swiftlang/llvm-project` commit.
- Cut to exercise the shared `publish-release.yml`, which now owns asset selection,
  `SHA256SUMS`, and the Release body for every repo in the family.
